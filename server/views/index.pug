doctype html
html.h-100(lang="en")
  head
    title= Title
    meta(name='viewport', content='width=device-width, initial-scale=1')
    link(href=assetPath('main.css') rel="stylesheet")
  body.h-100.bg-light
    div.h-100
      .h-100#chat
    script(src=`https://unpkg.com/react@18/umd/react.${isProduction ? 'production' : 'development'}.js`)
    script(src=`https://unpkg.com/react-dom@18/umd/react-dom.${isProduction ? 'production' : 'development'}.js`)
    script(src=assetPath('main.js'))